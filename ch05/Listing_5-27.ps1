# Listing 5-27 Create a production build by executing npm run build

npm run build

# Look at the build/static/css folder. Notice the Tailwind styles at the start of the file

# An important point is that Tailwind does'nt add all its CSS classes - that would 
# produce a masive CSS file! Instead, it only adds the CSS classes used in the app.